---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: mongodbdatabases.mongodb.shippercd.io
spec:
  group: mongodb.shippercd.io
  names:
    kind: MongodbDatabase
    listKind: MongodbDatabaseList
    plural: mongodbdatabases
    singular: mongodbdatabase
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MongodbDatabase is the Schema for the mongodbdatabases API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of MongodbDatabase
            properties:
              collections:
                items:
                  type: string
                type: array
              name:
                type: string
            required:
            - name
            type: object
          status:
            description: status defines the observed state of MongodbDatabase
            properties:
              createdResources:
                items:
                  type: string
                type: array
              deletedResources:
                items:
                  type: string
                type: array
              error:
                type: string
              failedResources:
                items:
                  type: string
                type: array
              message:
                type: string
              pendingResources:
                items:
                  type: string
                type: array
              ready:
                type: boolean
              updatedResources:
                items:
                  type: string
                type: array
            required:
            - createdResources
            - deletedResources
            - error
            - failedResources
            - message
            - pendingResources
            - ready
            - updatedResources
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
